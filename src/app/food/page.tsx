'use client'; import Link from 'next/link'; import { useEffect,useState } from 'react'; import { ArrowLeft, CheckCircle2, Clock3, ShoppingBag, Utensils } from 'lucide-react';
export default function Food(){const [items,setItems]=useState<any[]>([]),[order,setOrder]=useState<any>(null);useEffect(()=>{setItems([{id:'f1',name:'Paneer Power Bowl',price:220,prep:10,vendor:'Fuel Dock Kitchen',slot:'12:40–12:50',remaining:8},{id:'f2',name:'Veggie Wrap',price:180,prep:8,vendor:'Fuel Dock Kitchen',slot:'12:50–1:00',remaining:4},{id:'f3',name:'Cold Coffee',price:120,prep:5,vendor:'Fuel Dock Café',slot:'1:00–1:10',remaining:6}])},[]);const buy=async(item:any)=>{const r=await fetch('/api/food/order',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({itemId:item.id,slot:item.slot})});setOrder((await r.json()).data)};return <main className="min-h-screen bg-[#f6f7fb]"><nav className="border-b bg-white"><div className="mx-auto max-w-6xl px-6 py-4"><Link href="/dashboard" className="inline-flex gap-2 font-bold"><ArrowLeft size={17}/> EventOS</Link></div></nav><div className="mx-auto max-w-6xl px-6 py-8"><p className="text-sm font-bold uppercase tracking-widest text-indigo-600">Phase 3 · Smart food</p><h1 className="mt-2 text-4xl font-black">Lunch without the queue.</h1><p className="mt-2 max-w-2xl text-slate-500">Order ahead, reserve a capacity-controlled pickup slot, and only head to The Fuel Dock when your order is ready.</p>{order&&<div className="mt-6 rounded-3xl border border-emerald-200 bg-emerald-50 p-5"><CheckCircle2 className="text-emerald-600"/><h2 className="mt-2 text-xl font-black">Order {order.id} placed</h2><p className="text-sm text-slate-600">{order.item.name} · {order.slot} · Mock payment successful</p><div className="mt-3 rounded-2xl bg-white p-4"><b>Pickup QR</b><div className="mt-2 font-mono text-2xl tracking-[.25em]">{order.id}</div></div></div>}<div className="mt-8 grid gap-4 md:grid-cols-3">{items.map(item=><div key={item.id} className="rounded-3xl border bg-white p-5 shadow-sm"><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-orange-50 text-orange-600"><Utensils/></div><h2 className="mt-4 font-black">{item.name}</h2><p className="mt-1 text-sm text-slate-500">{item.vendor} · The Fuel Dock</p><div className="mt-5 grid grid-cols-2 gap-2 text-sm"><div className="rounded-xl bg-slate-50 p-3"><b>₹{item.price}</b><div className="text-xs text-slate-400">mock total</div></div><div className="rounded-xl bg-slate-50 p-3"><b>{item.remaining}/10</b><div className="text-xs text-slate-400">slot capacity</div></div></div><div className="mt-3 rounded-xl border p-3 text-sm"><Clock3 className="mr-1 inline" size={14}/> Pickup {item.slot}</div><button onClick={()=>buy(item)} className="mt-4 w-full rounded-2xl bg-slate-950 py-3 font-bold text-white"><ShoppingBag className="mr-2 inline" size={16}/> Order & reserve</button></div>)}</div><p className="mt-6 text-xs text-slate-400">Food vendors, menu items, prices and capacity are EventOS simulated demo data. Payment is mocked and not a live transaction.</p></div></main>}
