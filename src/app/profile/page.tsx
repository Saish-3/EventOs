'use client';
import { useState } from 'react'; import Link from 'next/link'; import { ArrowLeft, Check, Plus, UserRound } from 'lucide-react'; import { demoUser } from '@/lib/demo-data';
export default function Profile(){const [interests,setInterests]=useState(demoUser.interests);const [goals,setGoals]=useState(demoUser.goals);const [newI,setNewI]=useState('');const [saved,setSaved]=useState(false);const add=()=>{if(newI.trim()){setInterests([...interests,newI.trim()]);setNewI('')}};const save=async()=>{localStorage.setItem('eventos-profile',JSON.stringify({interests,goals}));setSaved(true);setTimeout(()=>setSaved(false),1800)};return <main className="min-h-screen bg-[#f6f7fb]"><nav className="border-b bg-white"><div className="mx-auto max-w-5xl px-6 py-4"><Link href="/dashboard" className="inline-flex items-center gap-2 font-bold"><ArrowLeft size={17}/> EventOS</Link></div></nav><div className="mx-auto max-w-5xl px-6 py-8"><div className="mb-7"><p className="text-sm font-bold uppercase tracking-widest text-indigo-600">Attendee profile</p><h1 className="mt-2 text-4xl font-black">Tell EventOS what matters to you.</h1></div><div className="grid gap-5 lg:grid-cols-[.7fr_1.3fr]"><div className="rounded-3xl bg-slate-950 p-7 text-white"><div className="flex h-16 w-16 items-center justify-center rounded-2xl bg-indigo-600"><UserRound/></div><h2 className="mt-5 text-2xl font-black">{demoUser.name}</h2><p className="mt-1 text-slate-300">{demoUser.jobTitle} · {demoUser.company}</p><p className="mt-5 text-sm leading-6 text-slate-400">Your preferences power EventOS recommendations. They stay demo-local in this prototype.</p></div><div className="rounded-3xl border bg-white p-7"><label className="text-sm font-bold">Interests</label><div className="mt-3 flex flex-wrap gap-2">{interests.map(x=><button key={x} onClick={()=>setInterests(interests.filter(i=>i!==x))} className="rounded-full bg-indigo-50 px-3 py-2 text-sm font-semibold text-indigo-700">{x} ×</button>)}</div><div className="mt-4 flex gap-2"><input value={newI} onChange={e=>setNewI(e.target.value)} onKeyDown={e=>e.key==='Enter'&&add()} placeholder="Add an interest" className="flex-1 rounded-2xl border px-4 py-3 outline-none focus:border-indigo-500"/><button onClick={add} className="rounded-2xl bg-slate-950 px-4 text-white"><Plus/></button></div><label className="mt-7 block text-sm font-bold">Goals</label><div className="mt-3 space-y-2">{goals.map(x=><button key={x} onClick={()=>setGoals(goals.filter(g=>g!==x))} className="block w-full rounded-2xl border px-4 py-3 text-left text-sm font-semibold">{x} <span className="float-right text-slate-400">×</span></button>)}</div><button onClick={save} className="mt-7 rounded-2xl bg-indigo-600 px-5 py-3 font-bold text-white">{saved?<><Check className="mr-1 inline" size={16}/> Saved</>:'Save preferences'}</button></div></div></div></main>}
