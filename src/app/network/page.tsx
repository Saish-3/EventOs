'use client'; import Link from 'next/link'; import { useEffect,useState } from 'react'; import { ArrowLeft, Building2, MessageCircle, Search, Sparkles, Users } from 'lucide-react';
export default function Network(){const [tab,setTab]=useState<'people'|'companies'>('people'),[q,setQ]=useState(''),[data,setData]=useState<any[]>([]),[sent,setSent]=useState<string|null>(null);useEffect(()=>{const url=tab==='people'?`/api/network/people?q=${encodeURIComponent(q)}`:`/api/network/companies?q=${encodeURIComponent(q)}`;fetch(url).then(r=>r.json()).then(j=>setData(j.data||[]))},[tab,q]);const hello=(p:any)=>{setSent(`Intro ready for ${p.name}`)};return <main className="min-h-screen bg-[#f6f7fb]"><nav className="border-b bg-white"><div className="mx-auto max-w-6xl px-6 py-4"><Link href="/dashboard" className="inline-flex gap-2 font-bold"><ArrowLeft size={17}/> EventOS</Link></div></nav><div className="mx-auto max-w-6xl px-6 py-8"><div className="rounded-3xl bg-slate-950 p-7 text-white"><div className="flex items-center gap-2 text-indigo-300"><Sparkles size={18}/> PHASE 3 · AI NETWORKING</div><h1 className="mt-3 max-w-3xl text-4xl font-black">Meet the right people — or the right company.</h1><p className="mt-2 max-w-2xl text-slate-300">EventOS ranks opt-in synthetic profiles and MTW showcase companies against your interests and goals.</p></div><div className="mt-6 flex flex-wrap gap-2"><button onClick={()=>setTab('people')} className={`rounded-xl px-4 py-2 font-bold ${tab==='people'?'bg-indigo-600 text-white':'border bg-white'}`}><Users className="mr-1 inline" size={15}/> Who should I meet?</button><button onClick={()=>setTab('companies')} className={`rounded-xl px-4 py-2 font-bold ${tab==='companies'?'bg-indigo-600 text-white':'border bg-white'}`}><Building2 className="mr-1 inline" size={15}/> Find a company</button><div className="flex min-w-[240px] flex-1 items-center rounded-xl border bg-white px-3"><Search size={16} className="text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder={tab==='people'?'Search role, company or interest':'Search startup or company'} className="w-full p-3 outline-none"/></div></div>{sent&&<div className="mt-4 rounded-2xl border border-indigo-200 bg-indigo-50 p-4 text-sm font-bold text-indigo-800">{sent}. Nothing was sent yet — review/edit the intro before sending.</div>}<div className="mt-6 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{data.map((p:any,i)=><div key={p.name||p} className="rounded-3xl border bg-white p-5 shadow-sm">{tab==='people'?<><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-indigo-50 text-indigo-600"><Users/></div><h2 className="mt-4 font-black">{p.name}</h2><p className="text-sm font-semibold">{p.role} · {p.company}</p><p className="mt-3 text-sm text-slate-500">{p.reason}</p><span className="mt-3 inline-block rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">{p.availability}</span><button onClick={()=>hello(p)} className="mt-4 w-full rounded-2xl bg-slate-950 py-3 font-bold text-white"><MessageCircle className="mr-1 inline" size={15}/> Say hello</button></>:<><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-indigo-50 text-indigo-600"><Building2/></div><h2 className="mt-4 font-black">{p}</h2><p className="mt-1 text-sm text-slate-500">MTW early-stage showcase</p><div className="mt-4 rounded-xl bg-slate-50 p-3 text-xs font-semibold">Explore company → synthetic attendee matches → booth/session context</div><button className="mt-4 w-full rounded-2xl border py-3 font-bold">Explore company</button></>}</div>)}</div><p className="mt-6 text-xs text-slate-400">All person profiles are synthetic and explicitly opt-in demo records. EventOS does not expose private contact details or invent real social profiles.</p></div></main>}
